export const metadata = {
  title: 'Register',
  description: 'Create your KrishTech Computers account to access enterprise IT tools, manage projects, and more.',
};

export default function RegisterLayout({ children }) {
  return children;
}

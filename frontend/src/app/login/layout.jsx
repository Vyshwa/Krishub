export const metadata = {
  title: 'Login',
  description: 'Sign in to your KrishTech Computers account to access the console, manage apps, and more.',
};

export default function LoginLayout({ children }) {
  return children;
}
